Node module for querying IMDB.com

Usage:

// List the episodes of the third season of Frasier.
var imdb = require('imdbjs');

imdb.episodes('tt0106004', 2, function(err, episodes) {
	console.log(episodes);	
});

Methods:

title(id, callback)

episodes(id, callback)
